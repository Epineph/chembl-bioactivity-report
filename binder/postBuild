#!/usr/bin/env bash
set -euo pipefail

# Ensure a predictable kernel name for Voila
python -m ipykernel install --name=python3 --display-name "Python (chembl-report)" --sys-prefix

# ipywidgets 8 + JupyterLab/Voila do not need the old classic Notebook
# nbextension command. Keeping postBuild minimal avoids Binder startup noise.
